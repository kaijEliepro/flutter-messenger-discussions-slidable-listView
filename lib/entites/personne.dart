class Personne {
  String  nom;
  String message;
  bool isVu;
  bool isConnected;
  bool isChat;
  int index;

  Personne(this.nom, this.message, this.isVu, this.isConnected, this.isChat,this.index);
}